import {StyleSheet} from 'react-native';

export const estilo = StyleSheet.create({
  fotoUsuario: {
    width: 40,
    height: 40,
    margin: 10,
    borderRadius: 30,
  },
  cabecalho: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});
